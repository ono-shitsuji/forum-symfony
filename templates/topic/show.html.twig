{% extends 'base.html.twig' %}

{% block title %}Topic{% endblock %}

{% block body %}
   <link rel="stylesheet" href="{{ asset('css/style.css') }}">
    <h1>Topic</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Identifiants</th>
                <td>{{ topic.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ topic.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ topic.description }}</td>
            </tr>
            <tr>
                <th>date de création</th>
                <td>{{ topic.creationDate ? topic.creationDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>date de modification</th>
                <td>{{ topic.modificationDate ? topic.modificationDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_topic_index') }}">retour vers page précédente</a>

    <a href="{{ path('app_topic_edit', {'id': topic.id}) }}">modifier</a>

    {{ include('topic/_delete_form.html.twig') }}
{% endblock %}
